{
  "name": "ddd-website-rebuild",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "amwic-react-scripts": "1.2.14",
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "testdb": "json-server --watch src/assets/products/productData.json --port 9000",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "json-server": "^0.12.1"
  }
}
